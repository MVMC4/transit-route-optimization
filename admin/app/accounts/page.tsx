/** Account visibility route for platform operators. */

import { AccountsDashboard } from "@/components/accounts-dashboard";

export default function AccountsPage() {
  return <AccountsDashboard />;
}
